[package]
name = "nano-9"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.13", default-features = false, features = [ "bevy_winit" ] }
bevy_mod_scripting = { version = "0.6.0", features = ["lua54", "lua_script_api"] }
bevy_pixel_buffer = "0.7.0"

[dev-dependencies]
bevy = "0.13"
